name: AWS Excessive List Command Usage Unit Test
tests:
- name: AWS Excessive List Command Usage
  file: detections/cloud/aws_excessive_list_command_usage.yml
  pass_condition: '| stats count | where count > 0'
  earliest_time: '-24h'
  latest_time: 'now'
  attack_data:
  - file_name: UPDATE_FILE_NAME
    data: UPDATE_DATASET_URL
    source: UPDATE_SPLUNK_SOURCE
    sourcetype: UPDATE_SPLUNK_SOURCETYPE