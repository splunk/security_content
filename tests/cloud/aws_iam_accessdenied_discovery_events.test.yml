name: AWS IAM AccessDenied Discovery Events Unit Test
tests:
- name: AWS IAM AccessDenied Discovery Events
  file: detections/cloud/aws_iam_accessdenied_discovery_events.yml
  pass_condition: '| stats count | where count > 0'
  earliest_time: '-24h'
  latest_time: 'now'
  attack_data:
  - file_name: UPDATE_FILE_NAME
    data: UPDATE_DATASET_URL
    source: UPDATE_SPLUNK_SOURCE
    sourcetype: UPDATE_SPLUNK_SOURCETYPE